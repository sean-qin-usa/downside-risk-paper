# Preprint copy

The same text as `submission/`, built for reading: single spacing, footnotes at the foot of the page, tables and figures in place. `paper_A_ssrn.pdf` is the manuscript and `paper_A_ssrn_online_appendix.pdf` the online appendix. `code/paper/make_ssrn_build.py` derives the sources from the journal-format ones.
